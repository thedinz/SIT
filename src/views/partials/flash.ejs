<% if (flash) { %>
  <div class="toast toast-<%= flash.type %>" role="status" data-toast>
    <span><%= flash.message %></span>
    <button type="button" aria-label="Dismiss message" data-dismiss-toast>×</button>
  </div>
<% } %>
